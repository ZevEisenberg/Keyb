source 'https://rubygems.org'

gem 'fastlane'
gem 'abbrev' # required by a dependency, but not included as part of the standard library in recent ruby versions
gem 'down'

# Used by build number script
gem 'git'
